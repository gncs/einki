# Dev environment defaults — safe to commit (no real secrets).
# Loaded by docker-compose.dev.yml (not auto-loaded; pass -f explicitly).
EINKI_USERNAME=admin
EINKI_PASSWORD=secret
EINKI_SECRET_KEY=dev-secret-key-not-for-production
